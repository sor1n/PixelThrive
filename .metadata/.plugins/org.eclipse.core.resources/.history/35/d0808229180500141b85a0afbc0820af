package com.base.pixelthrive.guis;

import net.PixelThrive.Client.Main;
import net.PixelThrive.Client.GUI.Text;

import org.newdawn.slick.Color;

import com.base.engine.Render;
import com.base.engine.Vector2f;
import com.base.engine.Window;
import com.base.pixelthrive.GUI;
import com.base.pixelthrive.PTGame;

public class MainMenu extends GUI
{
	
	public MainMenu()
	{
		
	}

	public void update(float delta)
	{
		if(active)
		{
			
		}
	}

	public void input(float delta)
	{
		if(active)
		{
			
		}
	}

	public void render()
	{
		if(active)
		{
			Render.pushMatrix();
			Render.drawRectangle(Vector2f.ZERO, Window.getSize(), new Color(60, 180, 220)); //background
			Render.drawCenteredStringFixedY(Vector2f.ZERO, new Vector2f(Window.getSize().getX(), 50), PTGame.TITLE, new Color(0xffffff), 57f);
			Render.drawString(Window.getWidth() - 40, Window.getHeight() - 10, PTGame.VERSION, new Color(0xffffff), 30f);
			Render.popMatrix();
		}
	}
}
